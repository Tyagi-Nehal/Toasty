# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Never commit real DB credentials — only .env.example (no secrets) is tracked
.env
.env.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea

# Claude Code local settings (per-machine, not shared)
.claude/settings.local.json

# Real club member data (names, attendance, role history) — one-time
# seed scripts already run against the live DB, and the original build
# brief. Kept locally for reference, not published on GitHub.
PROJECT_BRIEF.md
supabase/seed-roster.sql
supabase/seed-attendance.sql
supabase/seed-meetings.sql
supabase/seed-member-renewals.sql

.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
